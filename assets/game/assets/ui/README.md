# UI

Optional UI assets.

| File | Description | Usage |
|------|-------------|--------|
| **dialog-box.png** | Dialog box background | Optional. Loaded as `dialog-box` if present. The game can draw the dialog box with Graphics/CSS instead. |

Audio is in `/assets/audio/` (to be added later).
